{
  "name": "provider-kimi",
  "version": "1.0.0",
  "type": "provider",
  "displayName": "Kimi (Moonshot AI)",
  "description": "Kimi models via Moonshot AI API (OpenAI-compatible)",
  "entry": "./index.ts",
  "config": {
    "type": "object",
    "properties": {
      "apiKey": { "type": "string", "description": "Moonshot API key (falls back to MOONSHOT_API_KEY env var)", "secret": true },
      "baseUrl": { "type": "string", "description": "API base URL (default: https://api.moonshot.ai/v1)" }
    }
  }
}
